%img{src: "/clickdummy/w6-3.jpg", width: "100%"}

:coffeescript
    $ ->
        $(document).click (event)->
            if event.offsetY < $(document).height()/2
                console.log "Click upper half"
            else
                console.log "Click lower half"
                window.location.href = "treasure_interaction"